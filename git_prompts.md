[//]: # (This prompt is useful for generating a pull request description for changes on a git branch. It ensures the output is in markdown format and adheres to a specific pull request template.)
Create a pull request description for changes on the _____ git branch against the _____ branch. The output should be in markdown source code, not rendered, so it can be easily copied into github. Also make sure to follow the @/.github/pull_request_template.md

[//]: # (Clears all local worktrees and their associated branches. Useful for cleanup after launching many AI agents in separate worktrees.)
Remove / delete all of my local git worktrees and their associated local git branches